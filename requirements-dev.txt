# Development-only dependencies (not needed in production)
# Install with: my_env\Scripts\pip.exe install -r requirements-dev.txt

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx==0.27.2          # async test client for FastAPI

# Code quality
ruff==0.8.0
pre-commit==4.0.1
