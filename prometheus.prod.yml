global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: omnisight_backend
    static_configs:
      # In production Docker network, backend service is reachable by name
      - targets: ['backend:8000']
    metrics_path: /metrics
