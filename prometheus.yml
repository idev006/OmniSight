global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: omnisight_backend
    static_configs:
      # host.docker.internal resolves to the Docker host — backend runs there in dev
      - targets: ['host.docker.internal:8000']
    metrics_path: /metrics
